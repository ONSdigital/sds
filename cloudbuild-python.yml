steps:
  - name: python
    id: "setup Python"
    entrypoint: pip
    args: [ "install", "-r", "requirements.txt" ]
    env: [ "PYTHONPATH=src" ]
  - name: python
    id: "lint"
    args:
      - "-c"
      - |
        black . --check
    entrypoint: /bin/bash